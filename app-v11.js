const DIFFICULTIES = {
  easy: { label: 'Easy', description: 'Straightforward recall with clear clues and short answer choices.' },
  medium: { label: 'Medium', description: 'Clear understanding questions with concise, comparable answer choices.' },
  hard: { label: 'Hard', description: 'More specific clues with closely related answer choices.' },
  god: { label: 'God mode', description: 'The closest distractors and no answer reveal until the end.' }
};

const MAX_FILE_BYTES = 20 * 1024 * 1024;
const MAX_TEXT_CHARS = 240000;
const MAX_FACTS = 260;
const HISTORY_LIMIT = 120;
const VALID_COUNTS = new Set([10,20,30]);
const MIN_FACTS_BY_COUNT = {10:6,20:8,30:10};
const STOP_WORDS = new Set(`the a an and or but if then than that this these those with from into onto over under about through between during before after their there where which while when what would should could have been being were will shall must also only very more most some such each both they them your our its his her not are was for you all can may does did has had any how who why other same use used using one two first second including include includes example examples following according however therefore of to in on at by as is be it we he she they our your`.split(/\s+/));
const BAD_TOPIC_START = /^(?:a|an|the|of|to|in|on|at|by|for|from|with|without|into|onto|over|under|through|during|before|after|between|and|or|but|if|then|than|that|this|these|those|they|them|it|its|he|she|we|you|there|here|however|therefore|also|because|although|while|when|where|which|who|what|why|how|as|is|are|was|were|be|been|being|has|have|had|do|does|did|will|would|can|could|should|may|might|must)\b/i;
const BAD_TOPIC_END = /\b(?:a|an|the|of|to|in|on|at|by|for|from|with|without|into|onto|over|under|through|during|before|after|between|and|or|but|if|than|that|this|these|those|is|are|was|were|has|have|had|can|could|should|would|will|may|might|must|typically|mainly|often|usually|generally|primarily|basically|commonly|also)\s*$/i;
const CLAUSE_VERB_RE = /\b(?:is|are|was|were|has|have|had|does|do|did|can|could|should|would|will|may|might|must|needs|need|wants|want|includes|include|provides|provide|uses|use|helps|help|focuses|focus|requires|require|allows|allow|enables|enable|creates|create|reduces|reduce|increases|increase|improves|improve|ensures|ensure|means|refers)\b/i;
const RELATION_RE = /^(.{2,75}?)\s+(?:is defined as|refers to|means|is|are)\s+(.{12,340})$/i;
const ACTION_RELATION_RE = /^(.{2,75}?)\s+(?:includes|involves|focuses on|aims to|helps|supports|provides|requires|enables|allows|improves|reduces|increases|covers|contains|represents|measures|tracks|monitors|evaluates|identifies|connects|manages|controls)\s+(.{12,340})$/i;
const GENERIC_HEADINGS = new Set(['definition','example','examples','objectives','advantages','process','introduction','overview','summary']);
const QUESTION_TEMPLATES = {
  easy:[c=>`Which term matches this statement?\n\n“${c}”`,c=>`Which topic is described below?\n\n“${c}”`,c=>`Choose the correct term for this point:\n\n“${c}”`,c=>`This statement is about which topic?\n\n“${c}”`],
  medium:[c=>`Which concept is described here?\n\n“${c}”`,c=>`This statement refers to which concept?\n\n“${c}”`,c=>`Which topic best matches this statement?\n\n“${c}”`,c=>`Identify the concept described below:\n\n“${c}”`],
  hard:[c=>`Which concept best fits this statement from your notes?\n\n“${c}”`,c=>`Which closely related topic does this point describe?\n\n“${c}”`,c=>`Identify the most accurate concept for this statement:\n\n“${c}”`,c=>`Which topic is specifically associated with this point?\n\n“${c}”`],
  god:[c=>`Which concept is most precisely supported by this statement?\n\n“${c}”`,c=>`Select the exact concept associated with this point:\n\n“${c}”`,c=>`Which closely related topic is the best match?\n\n“${c}”`,c=>`Identify the most precise answer from the document:\n\n“${c}”`]
};

function cleanText(v=''){return String(v).replace(/\u00ad/g,'').replace(/\r\n?/g,'\n').replace(/[\t ]+/g,' ').replace(/\n{3,}/g,'\n\n').trim()}
function cleanLine(v=''){return String(v).replace(/\u00ad/g,'').replace(/\s+/g,' ').replace(/\s+([,.;:!?])/g,'$1').trim()}
function stripBullet(v=''){return cleanLine(v).replace(/^\s*(?:[•▪▫◦●○■□◆◇►▸‣]|[-–—])\s*/,'').replace(/^\s*\(?\d{1,2}\)?[.)]\s*/,'').trim()}
function stripNumbering(v=''){return cleanLine(v).replace(/^\s*\(?\d{1,2}\)?[.)]\s*/,'').trim()}
function normalizeKey(v=''){return String(v).toLowerCase().replace(/[“”‘’'"`]/g,'').replace(/[^a-z0-9]+/g,' ').trim()}
function cap(v=''){const s=String(v).trim();return s?s.charAt(0).toUpperCase()+s.slice(1):s}
function truncate(v,limit=120){const s=cleanLine(v);if(s.length<=limit)return s;const cut=s.slice(0,limit-1).replace(/\s+\S*$/,'').trim();return `${cut||s.slice(0,limit-1)}…`}
function isBoilerplate(line){const s=cleanLine(line);return !s||/^department of commerce\b/i.test(s)||/\bMAHE\b.*\bManipal\b.*\bEmail:/i.test(s)||/^https?:\/\//i.test(s)||/^www\./i.test(s)||/^\d{1,3}$/.test(s)||/^unit\s+\d+$/i.test(s)}
function topicWords(v){return stripNumbering(String(v).replace(/[:;]+$/,'')).split(/\s+/).filter(Boolean)}
function isTopicLabel(v){const s=stripNumbering(stripBullet(v)).replace(/[:;]+$/,'').trim();if(!s||s.length<2||s.length>58)return false;const w=topicWords(s);if(!w.length||w.length>9)return false;if(BAD_TOPIC_START.test(s)||BAD_TOPIC_END.test(s))return false;if(/[,!?;:]/.test(s)||/^\([^()]+\)$/.test(s))return false;if(/^[a-z]/.test(s)&&w.length>3)return false;if(CLAUSE_VERB_RE.test(s)&&w.length>3)return false;if(/\b(?:stage|step)\b/i.test(s)&&/^(?:the|in|this|here)\b/i.test(s))return false;if(/\b(?:product|products|service|services|goods|materials)\s*$/i.test(s)&&w.length>5)return false;return w.some(x=>/[A-Za-z]{2}/.test(x)&&!STOP_WORDS.has(x.toLowerCase()))}
function titleCaseRatio(v){const w=topicWords(v).filter(x=>/[A-Za-z]/.test(x));if(!w.length)return 0;return w.filter(x=>/^[A-Z][A-Za-z0-9&()/-]*$/.test(x)||/^[A-Z]{2,}$/.test(x)).length/w.length}
function looksLikeHeading(raw){const o=cleanLine(raw);if(!o||isBoilerplate(o))return false;const numbered=/^\s*\d{1,2}[.)]\s+/.test(o),colon=/:\s*$/.test(o);const s=stripNumbering(stripBullet(o)).replace(/[:;]+$/,'').trim();if(!isTopicLabel(s))return false;if(numbered||colon)return true;if(/[.!?]$/.test(o))return false;if(topicWords(s).length<=6&&titleCaseRatio(s)>=.55)return true;return /^[A-Z0-9 &()\-/]+$/.test(s)&&s.length<=60}
function cleanTopic(v){return stripNumbering(stripBullet(v)).replace(/[:;]+$/,'').trim()}
function isBulletLine(v){return /^\s*(?:[•▪▫◦●○■□◆◇►▸‣]|[-–—])\s*/.test(String(v))}
function makeBlocks(lines){const blocks=[];let cur=null;const flush=()=>{if(cur?.text)blocks.push(cur);cur=null};for(const raw of lines){const line=cleanLine(raw);if(!line||isBoilerplate(line))continue;if(looksLikeHeading(line)){flush();blocks.push({type:'heading',text:cleanTopic(line),raw:line});continue}if(isBulletLine(line)){flush();cur={type:'bullet',text:stripBullet(line),raw:line};continue}if(/^\s*\d{1,2}[.)]\s+/.test(line)){flush();cur={type:'bullet',text:stripNumbering(line),raw:line};continue}if(cur&&cur.type!=='heading'){cur.text=cleanLine(`${cur.text} ${line}`);cur.raw=cleanLine(`${cur.raw} ${line}`)}else{flush();cur={type:'paragraph',text:line,raw:line}}}flush();return blocks}
function splitLabelDescription(text){const s=cleanLine(text),m=s.match(/^(.{2,65}?)\s*[:–—-]\s+(.{8,300})$/);if(!m)return null;const subject=cleanTopic(m[1]),description=cleanLine(m[2]);if(!isTopicLabel(subject)||description.split(/\s+/).length<3)return null;return{subject,description}}
function deriveRelation(text){const s=cleanLine(text).replace(/[.!?]+$/,''),m=s.match(RELATION_RE)||s.match(ACTION_RELATION_RE);if(!m)return null;let subject=cleanTopic(m[1]);subject=subject.replace(/\s+(?:typically|mainly|often|usually|generally|primarily|basically|commonly|also)$/i,'').trim();const description=cleanLine(m[2]);if(!isTopicLabel(subject)||description.split(/\s+/).length<3)return null;return{subject,description}}
function factKey(f){return `${normalizeKey(f.subject)}::${normalizeKey(f.description)}`}
function looksLikeTitleFragment(v){const s=cleanLine(v),w=s.split(/\s+/).filter(Boolean);return w.length>=2&&w.length<=12&&!/[.!?]/.test(s)&&titleCaseRatio(s)>=.65}
function addFact(target,seen,fact){const subject=cleanTopic(fact.subject),description=truncate(fact.description,170).replace(/[.!?]+$/,'').trim(),group=cleanTopic(fact.group||'');if(!isTopicLabel(subject)||!description||description.split(/\s+/).length<3||isBoilerplate(description))return;if(/[,:;–—-]$/.test(description)||looksLikeTitleFragment(description))return;const sk=normalizeKey(subject),dk=normalizeKey(description);if(!sk||!dk||sk===dk||BAD_TOPIC_START.test(subject)||BAD_TOPIC_END.test(subject))return;const key=`${sk}::${dk}`;if(seen.has(key))return;seen.add(key);target.push({subject,description,group:isTopicLabel(group)?group:'',source:cleanLine(fact.source||fact.description),kind:fact.kind||'document'})}
function extractFactsFromPage(pageText,facts,seen){const lines=String(pageText).split(/\n+/).map(cleanLine).filter(Boolean),blocks=makeBlocks(lines);if(!blocks.length)return;const headings=blocks.filter(b=>b.type==='heading');let pageGroup='';for(const h of headings){const k=normalizeKey(h.text);if(!GENERIC_HEADINGS.has(k)&&isTopicLabel(h.text)){pageGroup=h.text;break}}let currentTopic=pageGroup,hasSubtopic=false;for(const b of blocks){if(b.type==='heading'){const h=cleanTopic(b.text);if(!pageGroup)pageGroup=h;if(normalizeKey(h)===normalizeKey(pageGroup)&&!hasSubtopic)currentTopic=pageGroup;else if(isTopicLabel(h)&&!GENERIC_HEADINGS.has(normalizeKey(h))){currentTopic=h;hasSubtopic=true}continue}const label=splitLabelDescription(b.text);if(label){addFact(facts,seen,{...label,group:pageGroup,source:b.text,kind:'label'});continue}const relation=deriveRelation(b.text);if(relation)addFact(facts,seen,{...relation,group:pageGroup,source:b.text,kind:'definition'});if(currentTopic&&isTopicLabel(currentTopic)){const desc=cleanLine(b.text);if(desc.length>=18&&desc.split(/\s+/).length>=4)addFact(facts,seen,{subject:currentTopic,description:desc,group:pageGroup,source:b.text,kind:hasSubtopic?'section':'page-topic'})}}}
export function extractFacts(text){const cleaned=cleanText(text).slice(0,MAX_TEXT_CHARS),facts=[],seen=new Set();for(const page of cleaned.split(/\f+/)){extractFactsFromPage(page,facts,seen);if(facts.length>=MAX_FACTS)break}if(facts.length<8){for(const sentence of cleaned.split(/(?<=[.!?])\s+|\n+/).map(cleanLine).filter(s=>s.length>=25&&s.length<=360&&!isBoilerplate(s))){const pair=splitLabelDescription(sentence)||deriveRelation(sentence);if(pair)addFact(facts,seen,{...pair,source:sentence,kind:'fallback'});if(facts.length>=MAX_FACTS)break}}return facts.slice(0,MAX_FACTS)}
function randomInt(max){return Math.floor(Math.random()*max)}
function shuffled(items){const a=[...items];for(let i=a.length-1;i>0;i--){const j=randomInt(i+1);[a[i],a[j]]=[a[j],a[i]]}return a}
function tokenSet(v){return new Set(normalizeKey(v).split(/\s+/).filter(w=>w&&!STOP_WORDS.has(w)))}
function lexicalSimilarity(a,b){const A=tokenSet(a),B=tokenSet(b);if(!A.size||!B.size)return 0;let o=0;for(const t of A)if(B.has(t))o++;return o/(A.size+B.size-o)}
function subjectSimilarity(a,b){return lexicalSimilarity(a,b)*4+1/(1+Math.abs(topicWords(a).length-topicWords(b).length))}
function uniqueSubjectFacts(facts){const seen=new Set(),out=[];for(const f of facts){const k=normalizeKey(f.subject);if(!k||seen.has(k)||!isTopicLabel(f.subject))continue;seen.add(k);out.push(f)}return out}
function pickDistractorSubjects(facts,correct,difficulty,amount=3){const ck=normalizeKey(correct.subject),gk=normalizeKey(correct.group||'');const candidates=uniqueSubjectFacts(facts).filter(f=>{const k=normalizeKey(f.subject);return k!==ck&&k!==gk&&isTopicLabel(f.subject)});const sameGroupSameKind=gk?candidates.filter(f=>normalizeKey(f.group)===gk&&f.kind===correct.kind):[];const sameGroup=gk?candidates.filter(f=>normalizeKey(f.group)===gk&&!sameGroupSameKind.includes(f)):[];const sameKind=candidates.filter(f=>f.kind===correct.kind&&!sameGroupSameKind.includes(f)&&!sameGroup.includes(f));const rest=candidates.filter(f=>!sameGroupSameKind.includes(f)&&!sameGroup.includes(f)&&!sameKind.includes(f));const rankClose=list=>[...list].sort((a,b)=>subjectSimilarity(correct.subject,b.subject)+lexicalSimilarity(correct.description,b.description)*2-(subjectSimilarity(correct.subject,a.subject)+lexicalSimilarity(correct.description,a.description)*2));let ordered;if(sameGroupSameKind.length>=amount){const sib=rankClose(sameGroupSameKind);ordered=difficulty==='easy'?shuffled(sib):[...shuffled(sib.slice(0,Math.min(sib.length,difficulty==='medium'?6:5))),...sib.slice(difficulty==='medium'?6:5)]}else if(difficulty==='easy')ordered=[...shuffled(sameGroupSameKind),...shuffled(sameGroup),...shuffled(sameKind),...shuffled(rest)];else{const pref=rankClose([...sameGroupSameKind,...sameGroup,...sameKind]),r=rankClose(rest),n=Math.min(pref.length,difficulty==='medium'?10:7);ordered=[...shuffled(pref.slice(0,n)),...pref.slice(n),...r]}const picked=[],used=new Set([ck]);for(const c of ordered){const k=normalizeKey(c.subject);if(!k||used.has(k))continue;used.add(k);picked.push(c.subject);if(picked.length===amount)break}return picked}
function makeClue(f,difficulty){const limits={easy:120,medium:105,hard:115,god:110};let clue=truncate(f.description,limits[difficulty]||105);const escaped=f.subject.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');if(escaped.length>=4)clue=clue.replace(new RegExp(`\\b${escaped}\\b`,'ig'),'this concept');return cap(clue)}
function optionSignature(options){return options.map(normalizeKey).sort().join('||')}
function questionSignature(f,prompt,options){return `${factKey(f)}::${normalizeKey(prompt)}::${optionSignature(options)}`}
function makeQuestion(facts,fact,difficulty,index,usedOptionSets,blockedQuestions,usedPrompts){for(let attempt=0;attempt<18;attempt++){const distractors=pickDistractorSubjects(facts,fact,difficulty,3);if(distractors.length<3)return null;const answer=fact.subject,clue=makeClue(fact,difficulty);if(!clue||clue.length<12)return null;const prompt=QUESTION_TEMPLATES[difficulty][randomInt(QUESTION_TEMPLATES[difficulty].length)](clue),pos=(index+randomInt(4))%4,options=[...distractors];options.splice(pos,0,answer);if(options.some(o=>!isTopicLabel(o)||o.length>58)||new Set(options.map(normalizeKey)).size!==4)continue;const os=optionSignature(options),ps=normalizeKey(prompt),qs=questionSignature(fact,prompt,options);if(usedOptionSets.has(os)||usedPrompts.has(ps)||blockedQuestions.has(qs))continue;return{prompt,answer,answerTopic:answer,options,correctIndex:pos,source:fact.source,difficulty,hint:difficulty==='easy'?`Hint: the answer starts with “${answer.charAt(0).toUpperCase()}”.`:null,signature:qs,optionSignature:os}}return null}
export function generateQuizFromFacts(facts,count=10,difficulty='medium',blockedSignatures=new Set()){if(!VALID_COUNTS.has(count))return[];if(!DIFFICULTIES[difficulty])difficulty='medium';if(!Array.isArray(facts)||facts.length<(MIN_FACTS_BY_COUNT[count]||6)||uniqueSubjectFacts(facts).length<4)return[];const pool=shuffled(facts.filter(f=>isTopicLabel(f.subject)).slice(0,MAX_FACTS)),usedOptionSets=new Set(),usedPrompts=new Set(),forbidden=new Set(blockedSignatures),quiz=[];let cursor=0;for(let pass=0;pass<18&&quiz.length<count;pass++){for(const fact of shuffled(pool)){const q=makeQuestion(pool,fact,difficulty,cursor++,usedOptionSets,forbidden,usedPrompts);if(!q)continue;usedOptionSets.add(q.optionSignature);usedPrompts.add(normalizeKey(q.prompt));forbidden.add(q.signature);quiz.push(q);if(quiz.length===count)break}}return quiz.length===count?quiz:[]}
function hashString(input){let h=0x811c9dc5;for(let i=0;i<input.length;i++){h^=input.charCodeAt(i);h=Math.imul(h,0x01000193)}return(h>>>0).toString(36)}
function docFingerprint(file,text){return `${hashString(text.slice(0,7000)+'|'+text.slice(-7000))}-${file.size}-${hashString(file.name.toLowerCase())}`}
function historyKey(fp){return `pagequiz-history-v3:${fp}`}
function readHistory(fp){if(typeof sessionStorage==='undefined')return new Set();try{return new Set((JSON.parse(sessionStorage.getItem(historyKey(fp))||'[]')||[]).slice(-HISTORY_LIMIT))}catch{return new Set()}}
function saveHistory(fp,quiz){if(typeof sessionStorage==='undefined')return;try{sessionStorage.setItem(historyKey(fp),JSON.stringify([...readHistory(fp),...quiz.map(q=>q.signature)].slice(-HISTORY_LIMIT))}catch{}}
let pdfjs=null,mammoth=null,JSZip=null;
async function loadPdfJs(){if(!pdfjs){pdfjs=await import('https://cdn.jsdelivr.net/npm/pdfjs-dist@6.3.289/build/pdf.min.mjs');pdfjs.GlobalWorkerOptions.workerSrc='https://cdn.jsdelivr.net/npm/pdfjs-dist@6.3.289/build/pdf.worker.min.mjs'}return pdfjs}
async function loadMammoth(){if(!mammoth){const m=await import('https://cdn.jsdelivr.net/npm/mammoth@1.12.3/+esm');mammoth=m.default||m}return mammoth}
async function loadJSZip(){if(!JSZip){const m=await import('https://cdn.jsdelivr.net/npm/jszip@3.10.2/+esm');JSZip=m.default||m}return JSZip}
function pdfItemsToLines(items){const positioned=items.filter(i=>i?.str&&i.str.trim()).map(i=>({text:i.str.trim(),x:Number(i.transform?.[4]||0),y:Number(i.transform?.[5]||0)})).sort((a,b)=>Math.abs(b.y-a.y)>2.2?b.y-a.y:a.x-b.x),rows=[];for(const item of positioned){let row=rows.find(r=>Math.abs(r.y-item.y)<=2.2);if(!row){row={y:item.y,items:[]};rows.push(row)}row.items.push(item)}rows.sort((a,b)=>b.y-a.y);return rows.map(r=>r.items.sort((a,b)=>a.x-b.x).map(i=>i.text).join(' ').replace(/\s+/g,' ').trim()).filter(Boolean)}
async function extractPdf(file,onProgress=()=>{}){const lib=await loadPdfJs(),data=new Uint8Array(await file.arrayBuffer()),task=lib.getDocument({data,disableAutoFetch:true,disableStream:true}),pdf=await task.promise,pageLimit=Math.min(pdf.numPages,160);let text='';try{for(let pageNo=1;pageNo<=pageLimit;pageNo++){onProgress(`Reading page ${pageNo} of ${pageLimit}…`);const page=await pdf.getPage(pageNo),content=await page.getTextContent({disableCombineTextItems:false}),lines=pdfItemsToLines(content.items);text+=`${lines.join('\n')}\n\f\n`;page.cleanup?.();if(text.length>=MAX_TEXT_CHARS)break;if(pageNo%6===0)await new Promise(r=>setTimeout(r,0))}}finally{try{pdf.cleanup?.()}catch{}try{await task.destroy?.()}catch{}}return text.slice(0,MAX_TEXT_CHARS)}
async function extractDocx(file,onProgress=()=>{}){onProgress('Reading Word document…');const lib=await loadMammoth(),r=await lib.extractRawText({arrayBuffer:await file.arrayBuffer()});return cleanText(r.value||'').slice(0,MAX_TEXT_CHARS)}
function decodeXml(t){return t.replace(/&#(x?[0-9a-f]+);/gi,(_,c)=>String.fromCodePoint(parseInt(c[0].toLowerCase()==='x'?c.slice(1):c,c[0].toLowerCase()==='x'?16:10))).replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"').replace(/&apos;/g,"'").replace(/&amp;/g,'&')}
async function extractPptx(file,onProgress=()=>{}){const Zip=await loadJSZip(),zip=await Zip.loadAsync(await file.arrayBuffer()),slides=Object.keys(zip.files).filter(n=>/^ppt\/slides\/slide\d+\.xml$/i.test(n)).sort((a,b)=>Number(a.match(/\d+/)?.[0]||0)-Number(b.match(/\d+/)?.[0]||0));if(!slides.length)throw new Error('This PowerPoint does not contain readable slides.');if(slides.length>150)throw new Error('Please use a presentation with 150 slides or fewer.');let text='';for(let i=0;i<slides.length;i++){onProgress(`Reading slide ${i+1} of ${slides.length}…`);const xml=await zip.file(slides[i]).async('string'),paras=[...xml.matchAll(/<a:p(?:\s[^>]*)?>([\s\S]*?)<\/a:p>/gi)].map(p=>[...p[1].matchAll(/<a:t(?:\s[^>]*)?>([\s\S]*?)<\/a:t>/gi)].map(t=>decodeXml(t[1])).join(' ').trim()).filter(Boolean);text+=`${paras.join('\n')}\n\f\n`;if(text.length>=MAX_TEXT_CHARS)break;if(i%10===0)await new Promise(r=>setTimeout(r,0))}return text.slice(0,MAX_TEXT_CHARS)}
async function extractText(file,onProgress){if(/\.pdf$/i.test(file.name))return extractPdf(file,onProgress);if(/\.docx$/i.test(file.name))return extractDocx(file,onProgress);if(/\.pptx$/i.test(file.name))return extractPptx(file,onProgress);throw new Error('Please choose a PDF, DOCX or PPTX file.')}
function escapeHtml(v){return String(v).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}

if(typeof document!=='undefined'){
  const app=document.querySelector('#app');let selectedFile=null,quiz=[],answers=[],currentIndex=0,selectedIndex=null,isBusy=false,difficulty='medium',questionCount=10,currentFingerprint='';
  const fileIcon='<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6M8 13h8M8 17h5"/></svg>';
  function setStatus(message=''){const s=document.querySelector('#status');if(s)s.innerHTML=message?`<div class="error">${escapeHtml(message)}</div>`:''}
  function home(){selectedFile=null;quiz=[];answers=[];currentIndex=0;selectedIndex=null;isBusy=false;document.onkeydown=null;app.innerHTML=`<p class="eyebrow">Notes in. Knowledge out.</p><h1>Turn your notes into<br><em>your next practice session.</em></h1><p class="sub">Upload a document. Take it one question at a time.</p><div class="layout"><section class="card"><label class="drop" id="drop"><span class="docicon">${fileIcon}</span><strong id="fileLabel">Drop your study material here</strong><span class="muted" id="fileHint">or <span class="browse">browse files</span> on your device</span><span class="muted">PDF, DOCX or PPTX · up to 20 MB</span><input id="file" type="file" accept=".pdf,.docx,.pptx"></label><div class="settings"><label for="count">Number of questions</label><select id="count"><option value="10" ${questionCount===10?'selected':''}>10 questions</option><option value="20" ${questionCount===20?'selected':''}>20 questions</option><option value="30" ${questionCount===30?'selected':''}>30 questions</option></select></div><fieldset class="difficulty"><legend>Choose your difficulty</legend><div class="mode-grid">${Object.entries(DIFFICULTIES).map(([k,m])=>`<label class="mode-choice"><input type="radio" name="difficulty" value="${k}" ${k===difficulty?'checked':''}><span>${m.label}</span></label>`).join('')}</div><p class="muted" id="modeHelp">${DIFFICULTIES[difficulty].description}</p></fieldset><button class="primary full" id="generate" disabled>Generate my quiz →</button><div id="status" role="status"></div><p class="notice">Questions and answer choices are generated only from readable text in your document. Scanned image-only pages need OCR first.</p></section><aside class="aside"><h2>A simple study rhythm</h2><div class="step"><span class="num">01</span><div><strong>Bring your notes</strong><small>Lecture notes, a chapter, or a study guide.</small></div></div><div class="step"><span class="num">02</span><div><strong>Pick your answer</strong><small>Every option is a short concept or term from the document.</small></div></div><div class="step"><span class="num">03</span><div><strong>See how you did</strong><small>Correct, wrong, and a review of your answers.</small></div></div><div class="privacy">No document history. Your file stays in your browser.</div></aside></div>`;const input=document.querySelector('#file'),drop=document.querySelector('#drop');input.addEventListener('change',()=>selectFile(input.files?.[0]));['dragenter','dragover'].forEach(t=>drop.addEventListener(t,e=>{e.preventDefault();drop.classList.add('drag')}));drop.addEventListener('dragleave',()=>drop.classList.remove('drag'));drop.addEventListener('drop',e=>{e.preventDefault();drop.classList.remove('drag');if(!isBusy)selectFile(e.dataTransfer.files?.[0])});document.querySelectorAll('[name=difficulty]').forEach(el=>el.addEventListener('change',()=>{difficulty=el.value;document.querySelector('#modeHelp').textContent=DIFFICULTIES[difficulty].description}));document.querySelector('#generate').addEventListener('click',generate)}
  function selectFile(file){selectedFile=null;document.querySelector('#generate').disabled=true;setStatus('');if(!file)return;if(!/\.(pdf|docx|pptx)$/i.test(file.name))return setStatus('Please choose a PDF, DOCX or PPTX file.');if(file.size>MAX_FILE_BYTES)return setStatus('This file is too large. Please upload a file smaller than 20 MB.');selectedFile=file;document.querySelector('#fileLabel').textContent=file.name;document.querySelector('#fileHint').textContent=`${(file.size/1024/1024).toFixed(2)} MB · Tap to change file`;document.querySelector('#generate').disabled=false}
  async function generate(){if(!selectedFile||isBusy)return;isBusy=true;const button=document.querySelector('#generate');questionCount=Number(document.querySelector('#count').value);const controls=document.querySelectorAll('#file,#count,[name=difficulty]');controls.forEach(el=>el.disabled=true);button.disabled=true;button.innerHTML='<span class="loading"></span>Reading document…';setStatus('');try{const text=await extractText(selectedFile,p=>button.innerHTML=`<span class="loading"></span>${escapeHtml(p)}`);if(cleanText(text).length<180)throw new Error('I could not find enough readable text. If this is a scanned PDF, use a text-based copy or run OCR first.');button.innerHTML='<span class="loading"></span>Building clean questions…';await new Promise(r=>setTimeout(r,0));const facts=extractFacts(text),topics=uniqueSubjectFacts(facts),required=MIN_FACTS_BY_COUNT[questionCount];if(facts.length<required||topics.length<4)throw new Error(`I found ${topics.length} reliable answer topics and ${facts.length} usable facts. This document needs at least 4 clear topics and more usable statements for a ${questionCount}-question quiz.`);currentFingerprint=docFingerprint(selectedFile,text);const blocked=readHistory(currentFingerprint);quiz=generateQuizFromFacts(facts,questionCount,difficulty,blocked);if(quiz.length!==questionCount&&blocked.size)quiz=generateQuizFromFacts(facts,questionCount,difficulty,new Set());if(quiz.length!==questionCount)throw new Error(`I could not create ${questionCount} clean, non-repeating questions from this document without lowering answer quality. Try fewer questions or a document with more distinct topics.`);saveHistory(currentFingerprint,quiz);answers=Array(questionCount).fill(null);currentIndex=0;selectedIndex=null;renderQuestion()}catch(e){console.error(e);setStatus(e?.message||'Something went wrong while reading this file.');controls.forEach(el=>el.disabled=false);button.disabled=false;button.textContent='Generate my quiz →';isBusy=false}}
  function renderQuestion(){isBusy=false;const q=quiz[currentIndex];selectedIndex=answers[currentIndex]?.selectedIndex??null;const answered=answers[currentIndex]!==null,godMode=difficulty==='god';app.innerHTML=`<section class="quiz"><div class="topline"><span class="filename">${escapeHtml(selectedFile?.name||'Document')}</span><span>Question ${currentIndex+1} of ${quiz.length} · ${DIFFICULTIES[difficulty].label}</span></div><div class="progress"><span style="width:${((currentIndex+1)/quiz.length)*100}%"></span></div><p class="eyebrow">${DIFFICULTIES[difficulty].label}</p><h2 class="qtitle">${escapeHtml(q.prompt)}</h2>${q.hint?`<div class="answer-hint">${escapeHtml(q.hint)}</div>`:''}<div class="options">${q.options.map((o,i)=>{let cls='option';if(selectedIndex===i)cls+=' selected';if(answered&&!godMode&&i===q.correctIndex)cls+=' correct';if(answered&&!godMode&&selectedIndex===i&&i!==q.correctIndex)cls+=' wrong';return `<button class="${cls}" data-index="${i}" ${answered?'disabled':''}><span class="letter">${String.fromCharCode(65+i)}</span><span>${escapeHtml(o)}</span></button>`}).join('')}</div>${answered&&!godMode?`<div class="feedback"><strong>${answers[currentIndex].correct?'Correct.':'Not quite.'}</strong><p>The correct answer is <b>${escapeHtml(q.answer)}</b>.</p></div>`:''}<div class="actions"><button class="secondary" id="quit">New document</button><button class="primary" id="next" ${answered?'':'disabled'}>${currentIndex===quiz.length-1?'See results':'Next question →'}</button></div></section>`;document.querySelectorAll('.option').forEach(b=>b.addEventListener('click',()=>chooseAnswer(Number(b.dataset.index))));document.querySelector('#next').addEventListener('click',nextQuestion);document.querySelector('#quit').addEventListener('click',home);document.onkeydown=e=>{if(/^[1-4]$/.test(e.key)&&!answers[currentIndex])chooseAnswer(Number(e.key)-1);if(e.key==='Enter'&&answers[currentIndex])nextQuestion()}}
  function chooseAnswer(i){if(answers[currentIndex]!==null)return;const q=quiz[currentIndex];answers[currentIndex]={selectedIndex:i,selectedText:q.options[i],correct:i===q.correctIndex};renderQuestion()}
  function nextQuestion(){if(answers[currentIndex]===null)return;if(currentIndex<quiz.length-1){currentIndex++;renderQuestion()}else renderResults()}
  function renderResults(){document.onkeydown=null;const correct=answers.filter(a=>a?.correct).length,wrong=quiz.length-correct,pct=Math.round(correct/quiz.length*100),message=pct>=90?'Excellent recall.':pct>=75?'Strong work.':pct>=55?'Good practice.':'Keep going — another round will help.';app.innerHTML=`<section class="quiz results"><div class="badge">✓</div><p class="eyebrow">Quiz complete</p><h1>${message}</h1><p class="sub">You answered ${correct} of ${quiz.length} questions correctly.</p><div class="score">${pct}%</div><div class="stats"><div class="stat"><b>${correct}</b><span>Correct</span></div><div class="stat wrong"><b>${wrong}</b><span>Wrong</span></div></div><div class="actions results-actions"><button class="secondary" id="newDoc">New document</button><button class="primary" id="again">Generate another quiz</button></div><details class="review"><summary>Review all ${quiz.length} answers</summary>${quiz.map((q,i)=>{const a=answers[i];return `<div class="review-item"><strong>${i+1}. ${escapeHtml(q.prompt)}</strong><p>Your answer: <b>${escapeHtml(a?.selectedText||'No answer')}</b> ${a?.correct?'✓':'✕'}</p>${a?.correct?'':`<p>Correct answer: <b>${escapeHtml(q.answer)}</b></p>`}<small>From your document: ${escapeHtml(q.source)}</small></div>`}).join('')}</details></section>`;document.querySelector('#newDoc').addEventListener('click',home);document.querySelector('#again').addEventListener('click',regenerateFromSameFile)}
  async function regenerateFromSameFile(){if(!selectedFile)return home();const file=selectedFile,count=questionCount,mode=difficulty;home();questionCount=count;difficulty=mode;selectFile(file);document.querySelector('#count').value=String(count);const radio=document.querySelector(`[name=difficulty][value="${mode}"]`);if(radio)radio.checked=true;document.querySelector('#modeHelp').textContent=DIFFICULTIES[mode].description;await generate()}
  home();
}
